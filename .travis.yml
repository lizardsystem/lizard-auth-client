language: python
python:
  - 2.7
  - 3.6
cache:
  directories:
    - eggs
    - $HOME/.local
sudo: false
install:
  - deactivate
  - pip install --user psycopg2
  - python bootstrap.py
  - bin/buildout
script:
  - bin/test
after_success:
  - bin/coveralls
